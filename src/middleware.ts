// ARQUIVO REMOVIDO — use src/proxy.ts
// Next.js 16 nao permite middleware.ts e proxy.ts simultaneamente.
// Toda a logica de proxy/middleware esta em src/proxy.ts.
// Este arquivo existe apenas para evitar erros de import em IDEs;
// deve ser deletado manualmente ou ignorado no build.
export {}
